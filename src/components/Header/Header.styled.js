import styled from 'styled-components';

export const AppBar = styled.header`
  padding: 20px 40px;
  box-shadow: 0px 8px 35px -4px rgba(13, 48, 224, 1);
  border-bottom: 1px solid ${p => p.theme.colors.border};
`;
